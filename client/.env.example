# GA4 評估 ID（Measurement ID，格式 G-XXXXXXXXXX）
# 沒設就完全不啟用 GA → 本機開發 / 未設定的環境不會送任何資料。
# 本機：複製成 client/.env 並填值（.env 已被 gitignore，不會進版控）。
# 部署：在 GitHub repo Settings → Secrets and variables → Actions 設 VITE_GA_ID，
#       build 時由 .github/workflows/deploy.yml 注入。
VITE_GA_ID=
